# Artifacts

Use `ARTIFACT_INDEX.jsonl` as the global append-only artifact index. Subagent workspaces live under mode-specific folders such as `proofs/`, `experiments/`, `code/`, and `route_checks/`; each workspace has an `artifact_manifest.json`.
